import Carousel from "Modules/Carousel/Carousel"; 

/**
 * Fader - Description
 * @extends Carousel
 */
export default class Fader extends Carousel {
  constructor(element) {
    super(element);
  }

  
}